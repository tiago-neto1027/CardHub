<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/listingsList"
            android:title="@string/browse_listings"
            android:icon="@drawable/ic_menu_listings"/>
        <item
            android:id="@+id/productList"
            android:title="@string/browse_product"
            android:icon="@drawable/ic_menu_products"/>
        <item
            android:id="@+id/cardsList"
            android:title="@string/browse_cards"
            android:icon="@drawable/ic_menu_cards"/>
    </group>
</menu>